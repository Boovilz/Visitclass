> **หมายเหตุ:** ถ้าต้องการวิธีที่ง่ายที่สุด แนะนำให้ใช้ [HOSTING.md](HOSTING.md) (Vercel + Neon) แทน
> เอกสารนี้สำหรับกรณีติดตั้งบนเซิร์ฟเวอร์ของตัวเอง

# คู่มือติดตั้งบนเซิร์ฟเวอร์จริง

## สิ่งที่ต้องมี

- **Node.js เวอร์ชัน 20 ขึ้นไป**
- เซิร์ฟเวอร์ Linux หรือ Windows ก็ได้ · RAM 512 MB ขึ้นไปเพียงพอ
- **ฐานข้อมูล PostgreSQL** — ใช้ Neon (ฟรี ไม่ต้องติดตั้ง) หรือ PostgreSQL ที่ติดตั้งเอง แล้วตั้ง `DATABASE_URL`

---

## ขั้นตอนติดตั้ง

### 1. อัปโหลดโค้ดขึ้นเซิร์ฟเวอร์

อัปโหลดทั้งโฟลเดอร์ **ยกเว้น** `node_modules/`, `data/`, `uploads/`, `backups/`, `logs/`

### 2. ติดตั้งและเตรียมไฟล์

```bash
npm ci --omit=dev && npm run setup
```

> `npm run setup` จะคัดลอกไลบรารีไป `public/vendor` และ build ไฟล์ CSS
> **ถ้าข้ามขั้นนี้หน้าเว็บจะแสดงผลผิดเพี้ยน**

### 3. สร้างข้อมูลตั้งต้น (ทำครั้งแรกครั้งเดียว)

```bash
SEED_PIN=xxxx npm run seed
```

เปลี่ยน `xxxx` เป็น PIN 4 หลักที่ต้องการ ห้ามใช้เลขซ้ำกันหมดหรือ 1234

> ถ้าย้ายระบบมาจากเครื่องเดิม ให้ข้ามขั้นนี้ แล้วคัดลอก `data/` กับ `uploads/` มาวางแทน
> จากนั้นรัน `npm run migrate` เพื่อเติมโครงสร้างใหม่ให้ข้อมูลเดิม

### 4. ตั้งค่าตัวแปรสภาพแวดล้อม

```bash
cp .env.example .env
```

ค่าที่ควรตั้งสำหรับใช้งานจริง:

| ตัวแปร | ค่าแนะนำ | เหตุผล |
| --- | --- | --- |
| `NODE_ENV` | `production` | ปิดข้อมูล debug ที่ไม่จำเป็น |
| `PORT` | `3000` | พอร์ตที่ reverse proxy ชี้มา |
| `SECURE_COOKIE` | `1` | **ตั้งเมื่อใช้ HTTPS** คุกกี้เซสชันจะไม่ถูกส่งผ่าน HTTP |

### 5. ตรวจความพร้อม

```bash
npm run preflight
```

ต้องได้ **ไม่ผ่าน 0** ก่อนเปิดใช้งานจริง — ระบบจะเตือนถ้ายังใช้ PIN เริ่มต้นหรือลืม build CSS

### 6. เปิดระบบให้ทำงานถาวร

**ทางเลือกที่ 1 — PM2 (ง่ายที่สุด)**

```bash
npm install -g pm2 && pm2 start ecosystem.config.js && pm2 save && pm2 startup
```

**ทางเลือกที่ 2 — systemd**

ใช้ไฟล์ตัวอย่าง `deploy/classroom-visit.service` (แก้ path และ user ให้ตรงกับเครื่องจริงก่อน)

```bash
sudo cp deploy/classroom-visit.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable --now classroom-visit
```

### 7. ตั้ง reverse proxy + HTTPS

ใช้ไฟล์ตัวอย่าง `deploy/nginx.conf.example`

```bash
sudo certbot --nginx -d visit.example.ac.th
```

> **สำคัญ:** ต้องตั้ง `client_max_body_size` อย่างน้อย 60M ไม่งั้นการอัปโหลดรูปหลายไฟล์จะล้มเหลว
> และหลังเปิด HTTPS แล้วอย่าลืมตั้ง `SECURE_COOKIE=1` แล้วรีสตาร์ตระบบ

---

## หลังติดตั้งเสร็จ

1. เปิดเว็บ กดปุ่มกุญแจมุมล่างขวา ใส่ PIN เข้าหน้าผู้ดูแล
2. ไปเมนู **ตั้งค่าระบบ** → แก้ชื่อโรงเรียน ต้นสังกัด อัปโหลดโลโก้ และ **เปลี่ยน PIN**
3. ตรวจเมนูข้อมูลพื้นฐานให้ตรงกับโรงเรียน (ภาคเรียน ปีการศึกษา ครู ผู้เยี่ยม ชั้นเรียน ตัวชี้วัด)
4. ทดลองบันทึกแบบประเมิน 1 รายการ แล้วกด **ดูเอกสาร / พิมพ์ PDF** เพื่อตรวจว่าเอกสารถูกต้อง

---

## การสำรองข้อมูล

```bash
npm run backup
```

สร้างสำเนาไว้ที่ `backups/<วันเวลา>/` ประกอบด้วยฐานข้อมูล รูปภาพ และกุญแจเซสชัน
ใช้ `VACUUM INTO` จึงสำรองได้แม้ระบบกำลังทำงานอยู่

**ตั้งให้สำรองอัตโนมัติทุกวันตี 2:**

```bash
0 2 * * * cd /var/www/classroom-visit && /usr/bin/npm run backup >> logs/backup.log 2>&1
```

> **ต้องสำรอง 3 อย่างคู่กันเสมอ:** `data/app.db` · `uploads/` · `data/.session-secret`
> ถ้าขาด `uploads/` รูปในรายงานเก่าจะหาย

---

## การอัปเดตระบบในอนาคต

```bash
npm run backup && git pull && npm ci --omit=dev && npm run setup && npm run migrate && pm2 restart classroom-visit
```

`npm run migrate` รันซ้ำได้ปลอดภัย — เพิ่มเฉพาะโครงสร้างที่ยังไม่มี ไม่ทับข้อมูลเดิม
ผู้ใช้ไม่ต้องล้าง cache เอง เพราะระบบใส่เลขเวอร์ชันไฟล์ให้อัตโนมัติ

---

## ข้อจำกัดที่ควรทราบ

| เรื่อง | รายละเอียด |
| --- | --- |
| **รันได้โปรเซสเดียว** | SQLite ไม่รองรับหลายโปรเซสเขียนพร้อมกัน ห้ามตั้ง PM2 เป็น cluster mode หรือ `instances > 1` |
| **จำนวนผู้ใช้** | เหมาะกับระดับโรงเรียน (ผู้ใช้พร้อมกันหลักสิบ) ถ้าต้องรองรับหลายโรงเรียนพร้อมกันควรย้ายไป PostgreSQL |
| **จำกัดการบันทึก** | ฟอร์มสาธารณะจำกัด 20 ครั้ง/10 นาที ต่อ IP เพื่อกันการยิงข้อมูลขยะ |
| **เซสชันผู้ดูแล** | หมดอายุใน 2 ชั่วโมง และหลุดทุกครั้งที่เปลี่ยน `SESSION_SECRET` |
| **ฟอนต์** | โหลดจาก Google Fonts ถ้าเซิร์ฟเวอร์ปิดอินเทอร์เน็ตขาออก ระบบจะใช้ฟอนต์ในเครื่องแทน (ยังอ่านได้ปกติ) |
