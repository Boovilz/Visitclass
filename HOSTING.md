# วิธีขึ้นเว็บบน Vercel

ใช้เวลาประมาณ **20 นาที** ทำผ่านหน้าเว็บทั้งหมด ไม่ต้องใช้ Linux ไม่ต้อง SSH

สร้างโปรเจกต์บน Vercel ก่อน แล้วค่อยสร้างฐานข้อมูลกับที่เก็บรูปจากในหน้า Vercel เลย
**Vercel จะใส่ค่าเชื่อมต่อให้อัตโนมัติ ไม่ต้องคัดลอกเอง**

| ส่วนประกอบ | หน้าที่ | ค่าใช้จ่าย |
| --- | --- | --- |
| **Vercel** | รันตัวเว็บ + HTTPS + โดเมน | Hobby ฟรี |
| **Neon** | ฐานข้อมูล PostgreSQL | ฟรี 0.5 GB |
| **Vercel Blob** | เก็บรูปภาพการเยี่ยมชั้นเรียน | มีโควตาฟรีให้ |

> ⚠️ **แผน Hobby ของ Vercel ห้ามใช้เชิงพาณิชย์** ระบบภายในโรงเรียนน่าจะเข้าข่ายใช้ได้
> แต่ควรอ่านเงื่อนไขก่อน ถ้าไม่แน่ใจต้องขึ้นแผน Pro ($20/เดือน)

---

## ขั้นที่ 1 — สร้างโปรเจกต์บน Vercel

1. เข้า <https://vercel.com/new> (ล็อกอินด้วย GitHub)
2. เลือก repository **`Visitclass`** → กด **Import**
3. **กด Deploy ได้เลย** ไม่ต้องตั้งค่าอะไร

รอประมาณ 2 นาที จะได้ลิงก์มา เช่น `https://visitclass.vercel.app`

> **เปิดแล้วจะเห็นหน้า "ตั้งค่าระบบยังไม่เสร็จ" — ถูกต้องแล้ว**
> เพราะยังไม่ได้ต่อฐานข้อมูล หน้านี้จะบอกขั้นตอนที่เหลือให้เอง

---

## ขั้นที่ 2 — สร้างฐานข้อมูล Neon จากใน Vercel

1. ในโปรเจกต์ → แท็บ **Storage** → **Create Database**
2. เลือก **Neon** (Serverless Postgres)
3. เลือก region **Singapore (ap-southeast-1)** — ใกล้ไทยที่สุด
4. กด **Create** → กด **Connect** เพื่อผูกเข้ากับโปรเจกต์

Vercel จะใส่ `DATABASE_URL` ให้อัตโนมัติ **ไม่ต้องคัดลอกเอง**

---

## ขั้นที่ 3 — สร้างที่เก็บรูปภาพ (Blob)

1. แท็บ **Storage** → **Create Database** → เลือก **Blob**
2. ตั้งชื่อ → **Create** → **Connect to Project**

Vercel จะใส่ `BLOB_READ_WRITE_TOKEN` ให้อัตโนมัติเช่นกัน

> **ถ้าข้ามขั้นนี้** ระบบยังทำงานได้ แต่รูปที่ครูอัปโหลดจะหายทุกครั้งที่ deploy

---

## ขั้นที่ 4 — ใส่ค่าที่ต้องตั้งเอง 3 ตัว

ไปที่ **Settings → Environment Variables** เพิ่มทีละตัว

| ชื่อ | ค่า |
| --- | --- |
| `SESSION_SECRET` | ค่าสุ่มยาว ๆ (ดูวิธีสร้างด้านล่าง) |
| `SECURE_COOKIE` | `1` |
| `SEED_PIN` | PIN 4 หลักที่ต้องการ เช่น `7391` |

สร้างค่า `SESSION_SECRET` ด้วยคำสั่งนี้ในเครื่อง แล้วคัดลอกผลลัพธ์ไปวาง

```bash
node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
```

> **ห้ามข้าม `SESSION_SECRET`** — ถ้าไม่ตั้ง ระบบจะไม่ยอมเริ่มทำงาน
> (ตั้งใจให้เป็นแบบนี้ เพราะถ้าไม่มีกุญแจถาวร ผู้ดูแลจะหลุดจากระบบเรื่อย ๆ โดยไม่รู้สาเหตุ)

---

## ขั้นที่ 5 — Redeploy แล้วเริ่มใช้งาน

1. แท็บ **Deployments** → เมนู **⋯** ของรายการล่าสุด → **Redeploy**
2. รอสักครู่แล้วเปิดลิงก์อีกครั้ง — คราวนี้จะเห็นหน้าแบบการเยี่ยมชั้นเรียนแล้ว

ระบบจะสร้างตารางและข้อมูลตั้งต้นให้อัตโนมัติ
(ภาคเรียน · ปีการศึกษา · ชั้นเรียน · กลุ่มสาระ 8 กลุ่ม · ตัวชี้วัด 10 ข้อ)

จากนั้น

1. กดปุ่ม **กุญแจ** มุมล่างขวา → ใส่ PIN ที่ตั้งไว้ใน `SEED_PIN`
2. เมนู **ตั้งค่าระบบ** → กรอกชื่อโรงเรียน ต้นสังกัด อัปโหลดโลโก้ และ **เปลี่ยน PIN**
3. เมนู **จัดการผู้รับผิดชอบชั้นเรียน** และ **จัดการผู้เยี่ยมชั้นเรียน** → กรอกรายชื่อจริง
4. ทดลองบันทึก 1 รายการ แล้วกด **ดูเอกสาร / พิมพ์ PDF**

---

## ย้ายข้อมูลเดิมจาก SQLite (ถ้ามี)

ถ้าเคยใช้ระบบเวอร์ชันก่อนหน้าและมีข้อมูลใน `data/app.db`

**หา `DATABASE_URL`:** Vercel → Storage → เลือก Neon → แท็บ **.env.local** → คัดลอกค่า
**หา `BLOB_READ_WRITE_TOKEN`:** Storage → เลือก Blob → แท็บ **.env.local**

```bash
DATABASE_URL="postgresql://..." BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..." npm run migrate:from-sqlite
```

ย้ายให้ครบทั้ง **ข้อมูลทุกตาราง · การตั้งค่า · PIN เดิม · รูปภาพขึ้น Blob**
และปรับลำดับ id ให้ต่อเนื่อง เพื่อไม่ให้ข้อมูลใหม่ชนกับของเดิม

> ปลายทางต้องเป็นฐานข้อมูล **เปล่า** ถ้ามีข้อมูลอยู่แล้วสคริปต์จะหยุดเพื่อกันข้อมูลซ้ำ
> ถ้าเปิดเว็บไปแล้วระบบจะสร้างข้อมูลตั้งต้นให้ ต้องล้างก่อนย้าย

---

## การอัปเดตในอนาคต

```bash
git add . && git commit -m "แก้ไข" && git push
```

Vercel deploy ใหม่ให้เอง ข้อมูลใน Neon และรูปใน Blob ไม่หาย
ถ้ามีการเพิ่มตาราง/คอลัมน์ ระบบปรับโครงสร้างให้เองตอนเริ่มทำงาน

---

## การสำรองข้อมูล

**Neon สำรองให้อัตโนมัติ** ย้อนเวลาได้ที่แท็บ **Backups** (แผนฟรีย้อนได้ 1 วัน)

ถ้าอยากเก็บสำเนาไว้เองด้วย

```bash
pg_dump "postgresql://..." > backup.sql
```

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุและวิธีแก้ |
| --- | --- |
| ขึ้นหน้า "ตั้งค่าระบบยังไม่เสร็จ" | ยังทำขั้นที่ 2–4 ไม่ครบ — หน้านั้นบอกไว้ว่าเหลืออะไร ทำแล้วกด Redeploy |
| ทำครบแล้วแต่ยังขึ้นหน้าเดิม | ยังไม่ได้ **Redeploy** — ตัวแปรใหม่จะมีผลหลัง deploy รอบถัดไปเท่านั้น |
| รูปหายหลัง deploy | ยังไม่ได้ต่อ Blob — ทำขั้นที่ 3 |
| เข้าหน้าผู้ดูแลไม่ได้ | PIN ไม่ตรง หรือเปลี่ยน `SESSION_SECRET` ทำให้เซสชันหลุด — ล็อกอินใหม่ |
| กรอก PIN ผิดหลายครั้งแล้วเข้าไม่ได้ | ระบบระงับชั่วคราว 5 นาที รอแล้วลองใหม่ |
| โหลดครั้งแรกช้า | Serverless + Neon แผนฟรีจะหลับเมื่อไม่มีคนใช้ คำขอแรกช้า 1–3 วินาที แล้วกลับมาปกติ |

---

## ข้อควรรู้

| เรื่อง | รายละเอียด |
| --- | --- |
| **ขนาดไฟล์อัปโหลด** | 5 MB ต่อรูป สูงสุด 10 รูปต่อการประเมิน 1 ครั้ง |
| **จำกัดการบันทึก** | ฟอร์มสาธารณะจำกัด 20 ครั้ง/10 นาที ต่อ IP กันการยิงข้อมูลขยะ |
| **เซสชันผู้ดูแล** | อายุ 2 ชั่วโมง |
| **โดเมนของโรงเรียน** | ผูกได้ที่ Settings → Domains (ฟรี รวม HTTPS) |
| **repo ควรเป็น Private** | ตั้งได้ที่ GitHub → Settings → General → Danger Zone |
