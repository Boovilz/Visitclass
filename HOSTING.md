# วิธีขึ้นเว็บบน Vercel + Neon + Blob

ใช้เวลาประมาณ **30 นาที** ทำผ่านหน้าเว็บทั้งหมด ไม่ต้องใช้ Linux ไม่ต้อง SSH

| ส่วนประกอบ | หน้าที่ | ค่าใช้จ่าย |
| --- | --- | --- |
| **Vercel** | รันตัวเว็บ + HTTPS + โดเมน | Hobby ฟรี |
| **Neon** | ฐานข้อมูล PostgreSQL | ฟรี 0.5 GB |
| **Vercel Blob** | เก็บรูปภาพการเยี่ยมชั้นเรียน | มีโควตาฟรีให้ |

> ⚠️ **แผน Hobby ของ Vercel ห้ามใช้เชิงพาณิชย์** ระบบภายในโรงเรียนน่าจะเข้าข่ายใช้ได้
> แต่ควรอ่านเงื่อนไขก่อน ถ้าไม่แน่ใจต้องขึ้นแผน Pro ($20/เดือน)

---

## ขั้นที่ 1 — เอาโค้ดขึ้น GitHub

เปิด **Git Bash** หรือ **PowerShell** ที่โฟลเดอร์โปรเจกต์

```bash
git add . && git commit -m "ระบบแบบเยี่ยมชั้นเรียน"
```

สร้าง repository ใหม่บน GitHub (ตั้งเป็น **Private** ได้) แล้วรัน

```bash
git remote add origin https://github.com/USERNAME/REPO.git
```

```bash
git branch -M main && git push -u origin main
```

> ไฟล์ `data/`, `uploads/` และ `.env` ไม่ถูกอัปโหลด (มี `.gitignore` กันไว้)

---

## ขั้นที่ 2 — สร้างฐานข้อมูลที่ Neon

1. สมัครที่ <https://neon.tech> (ล็อกอินด้วย GitHub ได้)
2. **Create project** → ตั้งชื่อ เช่น `classroom-visit`
3. เลือก region **Singapore (ap-southeast-1)** — ใกล้ไทยที่สุด
4. คัดลอก **Connection string** เก็บไว้ หน้าตาแบบนี้

```
postgresql://user:password@ep-xxxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
```

---

## ขั้นที่ 3 — สร้างโปรเจกต์บน Vercel

1. เข้า <https://vercel.com/new> → เลือก repository ที่ push ขึ้นไป
2. **อย่าเพิ่งกด Deploy** — เปิดหัวข้อ **Environment Variables** แล้วใส่ค่าเหล่านี้ก่อน

| ชื่อ | ค่า |
| --- | --- |
| `DATABASE_URL` | connection string จาก Neon (ขั้นที่ 2) |
| `SESSION_SECRET` | สร้างด้วยคำสั่งด้านล่าง |
| `SECURE_COOKIE` | `1` |
| `SEED_PIN` | PIN 4 หลักที่ต้องการ เช่น `7391` |

สร้างค่า `SESSION_SECRET` ด้วยคำสั่งนี้ในเครื่องแล้วคัดลอกผลลัพธ์ไปวาง

```bash
node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
```

3. กด **Deploy** รอประมาณ 2–3 นาที

---

## ขั้นที่ 4 — เปิดที่เก็บรูปภาพ (Blob)

1. ในโปรเจกต์บน Vercel → แท็บ **Storage** → **Create Database** → เลือก **Blob**
2. ตั้งชื่อแล้วกด Create → กด **Connect to Project**

Vercel จะเพิ่มตัวแปร `BLOB_READ_WRITE_TOKEN` ให้อัตโนมัติ

3. ไปแท็บ **Deployments** → กด **Redeploy** เพื่อให้ระบบรับค่าใหม่

> **ถ้าข้ามขั้นนี้** ระบบจะยังทำงานได้ แต่รูปที่ครูอัปโหลดจะหายทุกครั้งที่ deploy

---

## ขั้นที่ 5 — เข้าใช้งานครั้งแรก

เปิดลิงก์ที่ Vercel ให้มา เช่น `https://classroom-visit.vercel.app`

ระบบจะสร้างตารางและข้อมูลตั้งต้นให้อัตโนมัติเมื่อมีคนเปิดหน้าแรกครั้งแรก
(ภาคเรียน · ปีการศึกษา · ชั้นเรียน · กลุ่มสาระ 8 กลุ่ม · ตัวชี้วัด 10 ข้อ)

1. กดปุ่ม **กุญแจ** มุมล่างขวา → ใส่ PIN ที่ตั้งไว้ใน `SEED_PIN`
2. เมนู **ตั้งค่าระบบ** → กรอกชื่อโรงเรียน ต้นสังกัด อัปโหลดโลโก้ และ **เปลี่ยน PIN**
3. เมนู **จัดการผู้รับผิดชอบชั้นเรียน** และ **จัดการผู้เยี่ยมชั้นเรียน** → กรอกรายชื่อจริง
4. ทดลองบันทึก 1 รายการ แล้วกด **ดูเอกสาร / พิมพ์ PDF**

---

## ย้ายข้อมูลเดิมจาก SQLite (ถ้ามี)

ถ้าเคยใช้ระบบเวอร์ชันก่อนหน้าและมีข้อมูลใน `data/app.db`

```bash
DATABASE_URL="postgresql://..." BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..." npm run migrate:from-sqlite
```

สคริปต์จะย้ายให้ครบทั้ง **ข้อมูลทุกตาราง · การตั้งค่า · PIN เดิม · รูปภาพขึ้น Blob**
และปรับลำดับ id ให้ต่อเนื่อง เพื่อไม่ให้ข้อมูลใหม่ชนกับของเดิม

> ปลายทางต้องเป็นฐานข้อมูล **เปล่า** ถ้ามีข้อมูลอยู่แล้วสคริปต์จะหยุดเพื่อกันข้อมูลซ้ำ

---

## การอัปเดตในอนาคต

```bash
git add . && git commit -m "แก้ไข" && git push
```

Vercel deploy ใหม่ให้เอง ข้อมูลใน Neon และรูปใน Blob ไม่หาย
ถ้ามีการเพิ่มตาราง/คอลัมน์ ระบบจะปรับโครงสร้างให้เองตอนเริ่มทำงาน

---

## การสำรองข้อมูล

**Neon สำรองให้อัตโนมัติ** และย้อนเวลาได้ (Point-in-time restore) ที่แท็บ **Backups**
แผนฟรีย้อนได้ 1 วัน ถ้าต้องการนานกว่านั้นต้องอัปเกรด

ถ้าอยากเก็บสำเนาไว้เองด้วย ใช้ `pg_dump`

```bash
pg_dump "postgresql://..." > backup.sql
```

---

## ข้อควรรู้

| เรื่อง | รายละเอียด |
| --- | --- |
| **โหลดครั้งแรกช้า** | แผน Hobby ใช้ serverless ถ้าไม่มีคนใช้สักพัก คำขอแรกจะช้า 1–3 วินาที |
| **Neon หลับ** | แผนฟรีฐานข้อมูลจะหลับเมื่อไม่มีการใช้งาน คำขอแรกจะช้าเล็กน้อยแล้วกลับมาปกติ |
| **ขนาดไฟล์อัปโหลด** | ตั้งไว้ 5 MB ต่อรูป สูงสุด 10 รูปต่อการประเมิน 1 ครั้ง |
| **จำกัดการบันทึก** | ฟอร์มสาธารณะจำกัด 20 ครั้ง/10 นาที ต่อ IP กันการยิงข้อมูลขยะ |
| **โดเมนของโรงเรียน** | ผูกได้ที่ Settings → Domains (ฟรี รวม HTTPS) |
| **เซสชันผู้ดูแล** | อายุ 2 ชั่วโมง และหลุดทุกครั้งที่เปลี่ยน `SESSION_SECRET` |

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุและวิธีแก้ |
| --- | --- |
| เปิดเว็บแล้วขึ้น error 500 | ยังไม่ได้ตั้ง `DATABASE_URL` หรือ `SESSION_SECRET` — ตรวจที่ Settings → Environment Variables แล้ว Redeploy |
| รูปหายหลัง deploy | ยังไม่ได้ต่อ Blob — ทำขั้นที่ 4 |
| เข้าหน้าผู้ดูแลไม่ได้ | PIN ไม่ตรง หรือเปลี่ยน `SESSION_SECRET` ทำให้เซสชันหลุด — ล็อกอินใหม่ |
| กรอก PIN ผิดหลายครั้งแล้วเข้าไม่ได้ | ระบบระงับชั่วคราว 5 นาที รอแล้วลองใหม่ |
